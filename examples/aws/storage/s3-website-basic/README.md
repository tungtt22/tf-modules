# AWS S3 Website Example

Shows how to configure the S3 module for static website hosting with public read access.

After apply, upload your site files such as `index.html` and `404.html` to the bucket, then use the `website_endpoint` output.
